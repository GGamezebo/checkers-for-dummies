# concepts/

AI-only design reference for **Checkers for Dummies**.

| File | Purpose |
|------|---------|
| `ART_DIRECTION.md` | Canonical mood, palette, layout, combat table |
| `arena_layout_mockup.png` | Arena + HUD + slingshot diagram |
| `interaction_table.png` | Pawn / sword / shield interaction matrix |

Godot ignores this folder (`.gdignore`). Never reference `res://concepts/` from game code — see `.cursor/rules/concepts.mdc`.
